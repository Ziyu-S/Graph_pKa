The dir contains all the data and code, start with installation of env by 
conda env create -f environment.yml
or 
pip install -r requirements.txt

The Net contains all the scripts while Data has all the processed data.
